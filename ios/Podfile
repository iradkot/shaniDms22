ENV['RCT_NEW_ARCH_ENABLED'] = '0'
# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

# Set the minimum iOS deployment target to 12.0
# Xcode 16 requires iOS 12.0 or newer; older deployment targets cause build failures
platform :ios, '12.0'
prepare_react_native_project!

# Keep the default (static library) setup unless explicitly overridden.
# If you need frameworks for a specific environment, set USE_FRAMEWORKS to
# "static" or "dynamic" and the linkage will be applied. Otherwise, no
# use_frameworks! is enforced, which is the most stable option for React Native.
linkage_env = ENV['USE_FRAMEWORKS']
if linkage_env
  linkage = linkage_env.to_sym
  Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
  use_frameworks! :linkage => linkage
else
  Pod::UI.puts "Configuring Pods without use_frameworks! (default RN setup)".green
end

target 'shaniDms22' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath],
    :hermes_enabled => true,
    # An absolute path to your application root.
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  post_install do |installer|
    installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '12.0'
      end
    end

    # https://github.com/facebook/react-native/blob/main/packages/react-native/scripts/react_native_pods.rb#L197-L202
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false,
      # :ccache_enabled => true
    )
  end
end
